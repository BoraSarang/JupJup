<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- R6: 권한 9종은 :app 단일 소유 (머지). SystemForegroundService는 라이브러리 lint 대응상 유지 -->

    <!-- 통합 JupJup 앱: 서비스 컴포넌트만 선언 (Application/MainActivity는 app 모듈 소유) -->
    <application>
        <!-- LAN 릴레이 서버: dataSync 6h/24h 타임아웃 회피 위해 specialUse (API 34+), 이하는 dataSync -->
        <service
            android:name=".server.HttpServerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="lan_http_relay_server" />
        </service>

        <!-- WorkManager setForegroundAsync용 — CrawlWorker 특승격이 specialUse 쓰므로 병기 -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync|specialUse"
            tools:ignore="MissingClass"
            tools:node="merge">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="crawl_foreground_worker" />
        </service>

        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>